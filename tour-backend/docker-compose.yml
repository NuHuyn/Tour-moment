version: '3.8'

services:
  # Container chạy Backend Node.js
  backend:
    build: .
    restart: always
    environment:
      - MONGODB_URI=mongodb+srv://23521111_db_user:7I4qmDcikBwmZTCd@cluster0.o1rmyai.mongodb.net/newtour?retryWrites=true&w=majority
      - BASE_URL=http://10.128.0.2
    volumes:
      - ./uploads:/app/uploads

  # Container chạy Nginx làm "Cổng chào"
  nginx:
    image: nginx:latest
    ports:
      - "80:80" # Android sẽ gọi vào cổng 80 này
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend